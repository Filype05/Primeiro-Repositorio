name: primeira-pipeline
on:
  push:
    branches: ["main"]
  workflow_dispatch:
jobs:
  primeiro-job:
    name: "Meu primeiro Job"
    runs-on: ubuntu-latest
    steps:
      - name: "Minha primeira ação"
        run: echo "Hello World !!!"

      - name: "Segunda ação"
        run: |
          echo "Você criou seu primeiro workflow !!!"
          echo "Parabéns !!!"

      - name: "Script python"
        run: |
          python -c "variavel = 'Hello World !!!'; print(variavel)"
